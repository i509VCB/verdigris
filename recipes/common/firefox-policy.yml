# yaml-language-server: $schema=https://schema.blue-build.org/module-stage-list-v1.json

modules:
  - type: files
    files:
      - source: system/etc/firefox/policies
        destination: /etc/firefox/policies
